import { Component } from './Component';

export class Footer extends Component {
  constructor(selector: string) {
    super(selector);
    this.template;
    this.render();
  }

  createTemplate() {
    return;
  }
}
